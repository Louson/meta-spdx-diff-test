header:
  version: 14

local_conf_header:
  kconfig-test1: |
    SRC_URI:append:pn-linux-yocto = " file://kconfig1.cfg"
    FILESEXTRAPATHS:prepend:pn-linux-yocto := "${TOPDIR}/../layers/meta-sbom-diff-test/kernel-config:"
