# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-recipes-test"
BBFILE_PATTERN_meta-recipes-test = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-recipes-test = "6"

LAYERDEPENDS_meta-recipes-test = "core"
LAYERSERIES_COMPAT_meta-recipes-test = "scarthgap"
