# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-sbom-diff-test"
BBFILE_PATTERN_meta-sbom-diff-test = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-sbom-diff-test = "6"

LAYERDEPENDS_meta-sbom-diff-test = "core"
LAYERSERIES_COMPAT_meta-sbom-diff-test = "scarthgap"
